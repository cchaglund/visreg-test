import { SelectedSettings } from './selected-settings';
import { SettingsOptions } from './settings-options';

export const TargettedTestSettings = () => {
    return (
        <div>
            <SelectedSettings/>
            <SettingsOptions/>
        </div>
    );
};
